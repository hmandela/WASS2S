# process_variable { #wass2s.process_variable }

```python
process_variable(
    fpath,
    var_key,
    clim_start,
    clim_end,
    target_date,
    ratio_mode='monthly',
    precip_source='era5',
    sst_source='era5',
)
```

Compute and plot monthly anomalies or precipitation ratios.