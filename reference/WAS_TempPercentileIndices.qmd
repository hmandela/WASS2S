# WAS_TempPercentileIndices { #wass2s.WAS_TempPercentileIndices }

```python
WAS_TempPercentileIndices(
    base_period,
    percentile=90,
    season=None,
    var_type='TMAX',
    extreme_type='hot',
    bootstrap_samples=10,
    min_base_years=15,
)
```

Implementation of ETCCDI temperature percentile-based extreme indices.

Computes the annual percentage of days/nights exceeding (hot) or falling below (cold)
a specified percentile threshold, using a 5-day centered window for percentile calculation
(standard ETCCDI methodology).

Supported ETCCDI Indices:
- **TX90p** / **TN90p**: Percentage of hot days (TX > 90th) / hot nights (TN > 90th)
- **TX10p** / **TN10p**: Percentage of cold days (TX < 10th) / cold nights (TN < 10th)
- Custom percentiles are also supported (e.g., TX95p, TN99p, TX05p)

Key Features:
- 5-day centered window for calendar-day percentiles (avoids day-of-year bias)
- Proper leap-year handling (Feb 29 mapped to Feb 28)
- Seasonal filtering option
- Bootstrap confidence intervals (optional)
- Works with both station (CDT format) and gridded (xarray) data

References:
- ETCCDI Climate Change Indices (2009)
- Zhang et al. (2011): Indices for monitoring changes in extremes
- Sillmann et al. (2013): Climate extremes indices in the CMIP5 ensemble

## Parameters {.doc-section .doc-section-parameters}

| Name              | Type         | Description                                                                                                                        | Default    |
|-------------------|--------------|------------------------------------------------------------------------------------------------------------------------------------|------------|
| base_period       | slice        | Climatological base period for percentile calculation, e.g. slice("1961", "1990")                                                  | _required_ |
| percentile        | float        | Percentile threshold: - For hot extremes: 90, 95, 99 (days above percentile) - For cold extremes: 10, 5, 1 (days below percentile) | `90`       |
| season            | list of int  | Months to include in analysis (e.g., [6,7,8] for JJA)                                                                              | `None`     |
| var_type          | (TMAX, TMIN) | Temperature variable: 'TMAX' (TX - daily max) or 'TMIN' (TN - daily min)                                                           | `'TMAX'`   |
| extreme_type      | (hot, cold)  | Type of extreme: 'hot' (above percentile) or 'cold' (below percentile)                                                             | `'hot'`    |
| bootstrap_samples | int          | Number of bootstrap samples for confidence interval calculation                                                                    | `10`       |
| min_base_years    | int          | Minimum number of years required in base period (warning if fewer)                                                                 | `15`       |

## Examples {.doc-section .doc-section-examples}

Standard TX90p (Warm days percentage):

```python
>>> calc = WAS_TempPercentileIndices(
...     base_period=slice("1961", "1990"),
...     percentile=90,
...     var_type='TMAX',
...     extreme_type='hot'
... )
>>> tx90p = calc.compute(tmax_da)
```

Cold nights (TN10p) for JJA season:

```python
>>> cold_nights = WAS_TempPercentileIndices(
...     base_period=slice("1961", "1990"),
...     percentile=10,
...     var_type='TMIN',
...     extreme_type='cold',
...     season=[6, 7, 8]
... ).compute(tmin_da)
```

## Methods

| Name | Description |
| --- | --- |
| [compute](#wass2s.WAS_TempPercentileIndices.compute) | Compute index for xarray data (gridded). |
| [compute_insitu](#wass2s.WAS_TempPercentileIndices.compute_insitu) | Compute index for in-situ (station) data. |
| [get_index_definition](#wass2s.WAS_TempPercentileIndices.get_index_definition) | Return index definition metadata. |
| [transform_cdt](#wass2s.WAS_TempPercentileIndices.transform_cdt) | Transform CDT format to long format DataFrame. |

### compute { #wass2s.WAS_TempPercentileIndices.compute }

```python
WAS_TempPercentileIndices.compute(ds, var_name=None, parallel=True, nb_cores=4)
```

Compute index for xarray data (gridded).

### compute_insitu { #wass2s.WAS_TempPercentileIndices.compute_insitu }

```python
WAS_TempPercentileIndices.compute_insitu(df_cdt, return_confidence=False)
```

Compute index for in-situ (station) data.

### get_index_definition { #wass2s.WAS_TempPercentileIndices.get_index_definition }

```python
WAS_TempPercentileIndices.get_index_definition()
```

Return index definition metadata.

### transform_cdt { #wass2s.WAS_TempPercentileIndices.transform_cdt }

```python
WAS_TempPercentileIndices.transform_cdt(df)
```

Transform CDT format to long format DataFrame.