# WAS_compute_onset { #wass2s.WAS_compute_onset }

```python
WAS_compute_onset(user_criteria=None)
```

Class for computing agricultural seasonal onset dates based on cumulative rainfall criteria.

This class implements a widely used onset detection method in West African agrometeorology,
particularly for Sahel and Sudanian zones. It supports multiple agro-climatic zones with
zone-specific onset criteria (start/end search dates, cumulative rainfall threshold,
maximum dry days allowed, rainy day threshold).

Key Features:
- Handles both station-based (CDT format) and gridded (xarray) daily rainfall data
- Automatically assigns zones based on mean annual rainfall and latitude (Sahel–Guinea)
- Parallel computation using Dask for large gridded datasets
- Produces onset dates in day-of-year format (1–365/366)

Standard Zones & Criteria (default):
0. Sahel100_0mm:   start 01-Jun, ≥10 mm, ≤25 dry days, end 30-Aug
1. Sahel200_100mm: start 15-May, ≥15 mm, ≤25 dry days, end 15-Aug
2. Sahel400_200mm: start 01-May, ≥15 mm, ≤20 dry days, end 31-Jul
3. Sahel600_400mm: start 15-Mar, ≥20 mm, ≤20 dry days, end 31-Jul
4. Soudan:         start 15-Mar, ≥20 mm, ≤10 dry days, end 31-Jul
5. Golfe Of Guinea: start 01-Feb, ≥20 mm, ≤10 dry days, end 15-Jun

References:
- AGRHYMET Regional Centre (various operational guidelines)
- Sivakumar (1988, 1991): Methodology for onset prediction in West Africa
- Common practice in West African national meteorological services

## Parameters {.doc-section .doc-section-parameters}

| Name          | Type   | Description                                                                                                                                                                                                                                | Default   |
|---------------|--------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------|
| user_criteria | dict   | Custom zone-specific onset criteria. If None, uses default dictionary. Expected format: {zone_id: {"zone_name": str, "start_search": "MM-DD", "cumulative": float, "number_dry_days": int, "thrd_rain_day": float, "end_search": "MM-DD"}} | `None`    |

## Methods

| Name | Description |
| --- | --- |
| [adjust_duplicates](#wass2s.WAS_compute_onset.adjust_duplicates) | If any values in the Series repeat, nudge them by a tiny increment |
| [compute](#wass2s.WAS_compute_onset.compute) | Compute onset dates for each pixel in a given daily rainfall DataArray |
| [day_of_year](#wass2s.WAS_compute_onset.day_of_year) | Given a year 'i' and a month-day string 'dem_rech1' (e.g., '07-23'), |
| [onset_function](#wass2s.WAS_compute_onset.onset_function) | Calculate the onset date of a season based on cumulative rainfall criteria. |
| [transform_cdt](#wass2s.WAS_compute_onset.transform_cdt) | Transform a DataFrame with: |

### adjust_duplicates { #wass2s.WAS_compute_onset.adjust_duplicates }

```python
WAS_compute_onset.adjust_duplicates(series, increment=1e-05)
```

If any values in the Series repeat, nudge them by a tiny increment
so that all are unique (to avoid indexing collisions).

### compute { #wass2s.WAS_compute_onset.compute }

```python
WAS_compute_onset.compute(daily_data, nb_cores)
```

Compute onset dates for each pixel in a given daily rainfall DataArray
using different criteria based on isohyet zones.

#### Parameters {.doc-section .doc-section-parameters}

| Name       | Type             | Description                              | Default    |
|------------|------------------|------------------------------------------|------------|
| daily_data | xarray.DataArray | Daily rainfall data, coords = (T, Y, X). | _required_ |
| nb_cores   | int              | Number of parallel processes to use.     | _required_ |

#### Returns {.doc-section .doc-section-returns}

| Name   | Type             | Description                                |
|--------|------------------|--------------------------------------------|
|        | xarray.DataArray | Array with onset dates computed per pixel. |

### day_of_year { #wass2s.WAS_compute_onset.day_of_year }

```python
WAS_compute_onset.day_of_year(i, dem_rech1)
```

Given a year 'i' and a month-day string 'dem_rech1' (e.g., '07-23'),
return the day of the year (1-based).

### onset_function { #wass2s.WAS_compute_onset.onset_function }

```python
WAS_compute_onset.onset_function(
    x,
    idebut,
    cumul,
    nbsec,
    jour_pluvieux,
    irch_fin,
)
```

Calculate the onset date of a season based on cumulative rainfall criteria.

#### Parameters {.doc-section .doc-section-parameters}

| Name          | Type         | Description                                         | Default    |
|---------------|--------------|-----------------------------------------------------|------------|
| x             | array - like | Daily rainfall or similar values.                   | _required_ |
| idebut        | int          | Start index to begin searching for the onset.       | _required_ |
| cumul         | float        | Cumulative rainfall threshold to trigger onset.     | _required_ |
| nbsec         | int          | Maximum number of dry days allowed in the sequence. | _required_ |
| jour_pluvieux | float        | Minimum rainfall to consider a day as rainy.        | _required_ |
| irch_fin      | int          | Maximum index limit for the onset.                  | _required_ |

#### Returns {.doc-section .doc-section-returns}

| Name   | Type         | Description                                        |
|--------|--------------|----------------------------------------------------|
|        | int or float | Index of the onset date or NaN if onset not found. |

### transform_cdt { #wass2s.WAS_compute_onset.transform_cdt }

```python
WAS_compute_onset.transform_cdt(df)
```

Transform a DataFrame with:
  - Row 0 = LON
  - Row 1 = LAT
  - Row 2 = ELEV
  - Rows 3+ = daily data (or any date) with 'ID' column containing dates.

Returns an xarray DataArray with coords = (T, Y, X), variable = 'Observation'.