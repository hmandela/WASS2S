# WAS_compute_onset_dry_spell { #wass2s.WAS_compute_onset_dry_spell }

```python
WAS_compute_onset_dry_spell(user_criteria=None)
```

Class for computing the **longest dry spell length after the onset** of the rainy season.

This class extends standard onset detection by calculating the maximum consecutive dry days
within a user-defined window (``nbjour``) following the detected onset date.

The onset detection follows the same cumulative rainfall + dry-day tolerance method used
in West African agrometeorology (AGRHYMET, national services), with zone-specific criteria.

Key Features:
- Computes onset date (same logic as ``WAS_compute_onset``)
- Then finds the longest dry spell (consecutive days ≤ ``thrd_rain_day`` mm) within the next ``nbjour`` days
- Supports station (CDT) and gridded (xarray) daily rainfall data
- Automatically assigns agro-climatic zones based on mean annual rainfall and latitude
- Parallel computation with Dask for large grids

Default Zones & Criteria:
0. Sahel100_0mm     → start 01-Jun, ≥10 mm, ≤25 dry days, check 40 days after onset
1. Sahel200_100mm   → start 15-May, ≥15 mm, ≤25 dry days, check 40 days
2. Sahel400_200mm   → start 01-May, ≥15 mm, ≤20 dry days, check 40 days
3. Sahel600_400mm   → start 15-Mar, ≥20 mm, ≤20 dry days, check 45 days
4. Soudan           → start 15-Mar, ≥20 mm, ≤10 dry days, check 50 days
5. Golfe of Guinea  → start 01-Feb, ≥20 mm, ≤10 dry days, check 50 days

References:
- AGRHYMET Regional Centre operational methodologies
- Sivakumar (1988, 1991): Onset and dry spell analysis in West Africa
- Common practice in Sahelian and Sudanian national meteorological services

## Parameters {.doc-section .doc-section-parameters}

| Name          | Type   | Description                                                                                                                                                                                                                                      | Default   |
|---------------|--------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------|
| user_criteria | dict   | Custom zone-specific criteria. If None, uses default dictionary. Expected keys: zone_id → {"zone_name": str, "start_search": "MM-DD", "cumulative": float, "number_dry_days": int, "thrd_rain_day": float, "end_search": "MM-DD", "nbjour": int} | `None`    |

## Methods

| Name | Description |
| --- | --- |
| [adjust_duplicates](#wass2s.WAS_compute_onset_dry_spell.adjust_duplicates) | If any values in the Series repeat, nudge them by a tiny increment |
| [compute](#wass2s.WAS_compute_onset_dry_spell.compute) | Compute the longest dry spell length after the onset for each pixel in a |
| [day_of_year](#wass2s.WAS_compute_onset_dry_spell.day_of_year) | Given a year 'i' and a month-day string 'dem_rech1' (e.g., '07-23'), |
| [dry_spell_onset_function](#wass2s.WAS_compute_onset_dry_spell.dry_spell_onset_function) | Calculate the onset date of a season based on cumulative rainfall criteria, and |
| [dry_spell_onset_function_](#wass2s.WAS_compute_onset_dry_spell.dry_spell_onset_function_) | Calculate the onset date of a season based on cumulative rainfall criteria, and |
| [transform_cdt](#wass2s.WAS_compute_onset_dry_spell.transform_cdt) | Transform a DataFrame with: |

### adjust_duplicates { #wass2s.WAS_compute_onset_dry_spell.adjust_duplicates }

```python
WAS_compute_onset_dry_spell.adjust_duplicates(series, increment=1e-05)
```

If any values in the Series repeat, nudge them by a tiny increment
so that all are unique (to avoid indexing collisions).

### compute { #wass2s.WAS_compute_onset_dry_spell.compute }

```python
WAS_compute_onset_dry_spell.compute(daily_data, nb_cores)
```

Compute the longest dry spell length after the onset for each pixel in a
given daily rainfall DataArray, using different criteria based on isohyet zones.

#### Parameters {.doc-section .doc-section-parameters}

| Name       | Type             | Description                              | Default    |
|------------|------------------|------------------------------------------|------------|
| daily_data | xarray.DataArray | Daily rainfall data, coords = (T, Y, X). | _required_ |
| nb_cores   | int              | Number of parallel processes to use.     | _required_ |

#### Returns {.doc-section .doc-section-returns}

| Name   | Type             | Description                                        |
|--------|------------------|----------------------------------------------------|
|        | xarray.DataArray | Array with the longest dry spell length per pixel. |

### day_of_year { #wass2s.WAS_compute_onset_dry_spell.day_of_year }

```python
WAS_compute_onset_dry_spell.day_of_year(i, dem_rech1)
```

Given a year 'i' and a month-day string 'dem_rech1' (e.g., '07-23'),
return the 1-based day of the year.

### dry_spell_onset_function { #wass2s.WAS_compute_onset_dry_spell.dry_spell_onset_function }

```python
WAS_compute_onset_dry_spell.dry_spell_onset_function(
    x,
    idebut,
    cumul,
    nbsec,
    jour_pluvieux,
    irch_fin,
    nbjour,
)
```

Calculate the onset date of a season based on cumulative rainfall criteria, and
determine the longest dry spell sequence within a specified period after the onset.

### dry_spell_onset_function_ { #wass2s.WAS_compute_onset_dry_spell.dry_spell_onset_function_ }

```python
WAS_compute_onset_dry_spell.dry_spell_onset_function_(
    x,
    idebut,
    cumul,
    nbsec,
    jour_pluvieux,
    irch_fin,
    nbjour,
)
```

Calculate the onset date of a season based on cumulative rainfall criteria, and
determine the longest dry spell sequence within a specified period after the onset.

#### Parameters {.doc-section .doc-section-parameters}

| Name          | Type         | Description                                                    | Default    |
|---------------|--------------|----------------------------------------------------------------|------------|
| x             | array - like | Daily rainfall or similar values.                              | _required_ |
| idebut        | int          | Start index to begin searching for the onset.                  | _required_ |
| cumul         | float        | Cumulative rainfall threshold to trigger onset.                | _required_ |
| nbsec         | int          | Maximum number of dry days allowed in the sequence.            | _required_ |
| jour_pluvieux | float        | Minimum rainfall to consider a day as rainy.                   | _required_ |
| irch_fin      | int          | Maximum index limit for the onset.                             | _required_ |
| nbjour        | int          | Number of days to check for the longest dry spell after onset. | _required_ |

#### Returns {.doc-section .doc-section-returns}

| Name   | Type   | Description                                                                     |
|--------|--------|---------------------------------------------------------------------------------|
|        | float  | Length of the longest dry spell sequence after onset or NaN if onset not found. |

### transform_cdt { #wass2s.WAS_compute_onset_dry_spell.transform_cdt }

```python
WAS_compute_onset_dry_spell.transform_cdt(df)
```

Transform a DataFrame with:
  - Row 0 = LON
  - Row 1 = LAT
  - Row 2 = ELEV
  - Rows 3+ = daily data (or any date) with 'ID' column containing dates.

Returns an xarray DataArray with coords = (T, Y, X), variable = 'Observation'.